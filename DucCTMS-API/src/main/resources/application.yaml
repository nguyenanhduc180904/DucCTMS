server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/doAnTotNghiep
    username: postgres
    password: 111111
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
